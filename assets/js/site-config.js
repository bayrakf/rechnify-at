/* rechnify site config — empty IDs = feature skipped */
window.RECHNIFY_CONFIG = {
  /* Plausible: create site at plausible.io, keep domain */
  plausibleDomain: 'rechnify.at',
  /* GA4 optional, e.g. 'G-XXXXXXXX' — leave '' to skip */
  ga4Id: '',
  /* AdSense in-article slot under result — leave '' for Auto Ads only */
  adsenseSlotInArticle: '',
  adsenseClient: 'ca-pub-5052220565736445'
};
